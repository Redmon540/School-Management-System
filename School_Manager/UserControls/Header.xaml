<UserControl x:Class="School_Manager.Header"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:School_Manager"
             mc:Ignorable="d">
    <Border CornerRadius="10"
            Background="{StaticResource SecondaryColorBrush}" 
            BorderBrush="{StaticResource PrimaryColorBrush}"
            BorderThickness="0 0 0 5">

        <Label Content="{Binding Heading,RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:Header}}}"
               Foreground="{StaticResource LightColorBrush}"
               FontSize="{StaticResource FontSizeRegular}"
               Margin="5"
               FontWeight="Bold"
               HorizontalContentAlignment="Center"/>
    </Border>
</UserControl>
