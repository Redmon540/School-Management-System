<UserControl x:Class="School_Manager.IDCardThumbnail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:School_Manager"
             mc:Ignorable="d">
    <Border Background="{StaticResource VeryDarkGrayBrush}"
            CornerRadius="10"
            Padding="10"
            Margin="5">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ContentControl Margin="5" Grid.Row="0" Content="{Binding CardFront}"/>
            <ContentControl Margin="5" Grid.Row="1" Content="{Binding CardBack}"/>
        </Grid>
    </Border>
</UserControl>
